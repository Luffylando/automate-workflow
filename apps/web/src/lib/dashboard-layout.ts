/** Fixed height for dashboard todos/users panels so content scrolls inside. */
export const DASHBOARD_PANEL_HEIGHT_CLASS = "h-[32rem]";

/** Scrollable body region inside a fixed-height dashboard panel. */
export const DASHBOARD_SCROLL_AREA_CLASS =
  "min-h-0 flex-1 overflow-y-auto pr-0.5";
